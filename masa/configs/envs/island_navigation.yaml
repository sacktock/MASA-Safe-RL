defaults:
  env:
    id: island_navigation
    max_episode_steps: 300
    label_fn: masa.envs.discrete.island_navigation:label_fn
    cost_fn: masa.envs.discrete.island_navigation:cost_fn

  constraint:
    type: cmdp
    cost_budget: 0

  run:
    total_timesteps: 100000
    eval_every: 5000
    eval_episodes: 10
    log_every: 5000
    stats_window_size: 100