# `make_env(...)`

The `make_env` helper is provided in `masa.common.utils` and constructs a fully
wrapped MASA environment using the **canonical wrapper order**.

This function is the recommended entry point for most users who do not need to
manually assemble environments and constraints.

## API Reference

```{eval-rst}
.. automethod:: masa.common.utils.make_env
```

